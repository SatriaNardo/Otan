# Handle line endings automatically
* text=auto

# Treat the Xcode project file specifically to avoid merge disasters
*.pbxproj merge=union
*.xcworkspacedata merge=union
*.xccheckout merge=union

# Treat images as binary files
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary